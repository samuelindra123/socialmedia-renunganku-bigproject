 `.env.development` (local dev)

   env
     # Database (dev)
     DATABASE_URL="postgresql://dev_user:dev_password@localhost:5432/dev_db"
     DATABASE_URL_POOLING="postgresql://dev_user:dev_password@localhost:6543/dev_db"

     # Resend (email)
     RESEND_API_KEY="re_dev_xxxxxxxxxxxxxxxxxxxxx"

     # DigitalOcean Spaces (storage)
     DO_SPACES_KEY="DEV_SPACES_KEY"
     DO_SPACES_SECRET="DEV_SPACES_SECRET"
     DO_SPACES_ENDPOINT="sgp1.digitaloceanspaces.com"
     DO_SPACES_BUCKET="renunganku-dev"
     DO_SPACES_CDN="renunganku-dev.sgp1.cdn.digitaloceanspaces.com"

     # JWT
     JWT_SECRET="dev-super-secret-key"
     JWT_EXPIRATION="7d"

     # URLs
     API_URL="http://localhost:4000"
     FRONTEND_URL="http://localhost:3000"

     # Google OAuth (dev)
     GOOGLE_CLIENT_ID="your-dev-google-client-id"
     GOOGLE_CLIENT_SECRET="your-dev-google-client-secret"
     GOOGLE_OAUTH_REDIRECT="http://localhost:4000/auth/google/callback"

   `.env.production` (server/production)

   env
     # Database (prod)
     DATABASE_URL="postgresql://prod_user:prod_password@prod-host:5432/prod_db"
     DATABASE_URL_POOLING="postgresql://prod_user:prod_password@prod-host:6543/prod_db"

     # Resend (email)
     RESEND_API_KEY="re_prod_xxxxxxxxxxxxxxxxxxxxx"

     # DigitalOcean Spaces (storage)
     DO_SPACES_KEY="PROD_SPACES_KEY"
     DO_SPACES_SECRET="PROD_SPACES_SECRET"
     DO_SPACES_ENDPOINT="sgp1.digitaloceanspaces.com"
     DO_SPACES_BUCKET="renunganku"
     DO_SPACES_CDN="renunganku.sgp1.cdn.digitaloceanspaces.com"

     # JWT
     JWT_SECRET="prod-super-secret-key-yang-kuat"
     JWT_EXPIRATION="7d"

     # URLs (sesuaikan dengan domain sebenarnya)
     API_URL="https://api.renunganku.com"
     FRONTEND_URL="https://app.renunganku.com"

     # Google OAuth (prod)
     GOOGLE_CLIENT_ID="your-prod-google-client-id"
     GOOGLE_CLIENT_SECRET="your-prod-google-client-secret"
     GOOGLE_OAUTH_REDIRECT="https://api.renunganku.com/auth/google/callback"